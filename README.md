
 

  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm install -D @types/react@18 @types/react-dom@18` to install react18

  Run `npm run dev` to start the development server.
  
